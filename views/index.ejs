<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
    <link
      rel="stylesheet"
      href="https://unicons.iconscout.com/release/v4.0.0/css/line.css"
    />
    <style>
      body {
        font-family: "Arial", sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f8f9fa;
        color: #495057;
      }
      header {
        background-color: #000;
        color: #ffffff;
        padding: 20px;
        font-size: 32px;
        position: relative;
        width: 100%;
      }
      .banner {
        background-color: #4285cb;
        height: 10px;
        width: 100%;
        padding: 20px;
        position: relative;
      }
      nav {
        background-color: #343a40;
        color: #ffffff;
        padding: 15px;
        width: 220px;
        position: absolute;
        height: 1150px;
        z-index: 1;
      }
      nav ul {
        list-style-type: none;
        padding: 0;
        margin: 0;
      }
      nav a {
        text-decoration: none;
        color: #ffffff;
        font-size: 18px;
        display: block;
        padding: 10px;
        transition: background-color 0.3s;
      }
      nav a:hover {
        background-color: #1d2124;
      }
      section {
        margin-left: 240px;
        padding: 20px;
        height: 100%;
        margin-bottom: auto;
        text-align: center;
      }
      .button-container {
        margin-top: 20px;
      }
      .button-container button {
        padding: 12px;
        width: 70%;
        box-sizing: border-box;
        cursor: pointer;
        border: none;
        border-radius: 4px;
        transition: background-color 0.3s;
        margin-bottom: 15px;
      }
      .button-container button.submit {
        background-color: #007bff;
        color: #ffffff;
      }
      .button-container button.cancel {
        background-color: #6c757d;
        color: #ffffff;
      }
      .thin-banner {
        background-color: #007bff;
        height: 2px;
        margin-top: -2px;
      }
    </style>
  </head>
  <body>
    <header>Home Page</header>
    <div class="banner"></div>
    <nav>
      <ul>
        <li>
          <a href="#import"><i class="uil uil-upload"></i> Data Intake Form</a>
        </li>
        <li>
          <a href="#data-control"
            ><i class="uil uil-shield-check"></i> Data Request Form</a
          >
        </li>
      </ul>
      <div class="thin-banner"></div>
    </nav>
    <section>
      <div class="button-container">
        <button type="button" class="submit" onclick="redirectToForm('intake')">
          Data Intake Form
        </button>
        <button
          type="button"
          class="cancel"
          onclick="redirectToForm('request')"
        >
          Data Request Form
        </button>
      </div>
      <script>
        function redirectToForm(formType) {
          if (formType === "intake") {
            window.location.href = "intakeform";
          } else if (formType === "request") {
            window.location.href = "datarequestform";
          }
        }
      </script>
    </section>
  </body>
</html>
